import React from "react";


/**
 * 舞台
 * 内容渲染区
 * @returns 
 */
const Stage: React.FC = () => {
    return <div>
        这个是渲染区
        1.一个用于展示渲染结果的canvas
        2.一个隐藏的存储rgb用于后期选择图形
    </div>
}

export default Stage;